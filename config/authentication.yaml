# DEV ONLY defaults. This file seeds config-local/ via `make init-config`
# for local development. Do not use these credentials in production.
# Change the admin password before any deployment.
users:
- permissions: admin
  password_hash: 8c6976e5b5410415bde908bd4dee15dfb167a9c873fc4bb8a81f6f2ab448a918  # admin
  username: admin
external_auth:
  allow_root: false
  enabled: false
  groups:
    admin_group: openmux_admin
    read_group: openmux_read
    write_group: openmux_write
  # helper: /usr/lib/openmux/openmux-pam-helper  # optional override
  service: openmux
  timeout: 10
